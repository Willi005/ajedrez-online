/**
 * The handful of Lucide glyphs the interface needs, inlined.
 *
 * The design system asks for Lucide (readme, "Icons"). It is inlined rather
 * than installed because four paths do not justify a dependency, and because a
 * bundled icon font is one more thing that can fail to load on a machine with
 * no internet — the same reason the two typefaces are vendored.
 *
 * Paths are Lucide's own, unchanged, on its 24x24 grid.
 */
const PATHS = {
  copy: (
    <>
      <rect width="14" height="14" x="8" y="8" rx="2" ry="2" />
      <path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2" />
    </>
  ),
  check: <path d="M20 6 9 17l-5-5" />,
  x: <path d="M18 6 6 18M6 6l12 12" />,
  flag: (
    <>
      <path d="M4 15s1-1 4-1 5 2 8 2 4-1 4-1V3s-1 1-4 1-5-2-8-2-4 1-4 1z" />
      <path d="M4 22v-7" />
    </>
  ),
}

export default function Icon({ name, size = 14 }) {
  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth="2"
      strokeLinecap="round"
      strokeLinejoin="round"
      aria-hidden="true"
      focusable="false"
    >
      {PATHS[name]}
    </svg>
  )
}
